export const metadata = {
  title: "App | DriveNet",
  description: "Run the DriveNet simulation",
};

export default function AppLayout({
  children,
}: {
  children: React.ReactNode;
}) {
	return children;
}